import 'package:flutter/material.dart';
import '../../../screens/screens.dart';

class SevenStepsView extends StatelessWidget {
  const SevenStepsView({super.key});

  @override
  Widget build(BuildContext context) => const SevenStepsScreen();
}